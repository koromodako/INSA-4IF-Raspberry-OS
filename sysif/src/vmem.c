#include "vmem.h"
#include "kheap.h"

unsigned int init_kern_translation_table(void)
{
	//uint32_t device_flags = 0b010000110111;

	//uint8_t * table1_addr = (uint8_t*)(kAlloc_aligned(FIRST_LVL_TT_SIZE, 0));

	//int page; 
	//for(page = 0; page < 0; ++page)
	//{

	//}
	return 0;
}